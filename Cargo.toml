[package]
name = "qubit-redact"
version = "0.5.0"
edition = "2024"
rust-version = "1.94"
authors = ["Haixing Hu <starfish.hu@gmail.com>"]
license = "Apache-2.0"
repository = "https://github.com/qubit-ltd/rs-redact"
description = "Rule-driven redaction for fields, diagnostics, HTTP data, and Rust domain objects"
homepage = "https://github.com/qubit-ltd/rs-redact"
documentation = "https://docs.rs/qubit-redact"
readme = "README.md"
keywords = ["redact", "mask", "privacy", "logging", "qubit"]
categories = ["development-tools", "value-formatting"]
include = [
    "/Cargo.toml",
    "/LICENSE",
    "/README.md",
    "/README.zh_CN.md",
    "/src/**",
    "/doc/**",
    "/tests/**",
    "/benches/**",
]

[package.metadata.docs.rs]
all-features = true

[lib]
name = "qubit_redact"
doctest = true

[workspace]
members = ["derive"]
default-members = [".", "derive"]
exclude = ["fuzz"]
resolver = "3"

[workspace.package]
version = "0.5.0"
edition = "2024"
rust-version = "1.94"
authors = ["Haixing Hu <starfish.hu@gmail.com>"]
license = "Apache-2.0"
repository = "https://github.com/qubit-ltd/rs-redact"
homepage = "https://github.com/qubit-ltd/rs-redact"
readme = "README.md"

[features]
default = []
serde = ["dep:serde"]
json = ["dep:serde_json"]
http = [
    "json",
    "dep:form_urlencoded",
    "dep:http",
    "dep:url",
]

[dependencies]
form_urlencoded = { version = "1.2", optional = true }
http = { version = "1.5", optional = true }
serde = { version = "1.0", optional = true }
serde_json = { version = "1.0", optional = true }
url = { version = "2.5", optional = true }

[dev-dependencies]
criterion = { version = "0.8", default-features = false }
indexmap = "2.14"
proptest = "1.11"
qubit-redact-derive = { path = "derive" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
trybuild = "1.0"

[[bench]]
name = "redaction_policy"
harness = false

[[bench]]
name = "http_diagnostics"
harness = false
required-features = ["http"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage)'] }
