[package]
name = "serde"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact = { path = "../../../../..", features = ["serde"] }
qubit-redact-derive = { path = "../../../.." }
