[package]
name = "qubit-redact-serde-disabled-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact = { path = "../../../../..", default-features = false, features = ["derive"] }
