[package]
name = "qubit-redact-serde-disabled-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact = {
    version = "0.6",
    path = "../../../../",
    default-features = false,
}
qubit-redact-derive = { version = "0.6", path = "../../../../derive" }
serde = "1"
